{
  "name": "docus-starter",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nuxi dev",
    "build": "nuxi build",
    "generate": "nuxi generate && node -e \"const fs = require('fs'); fs.writeFileSync('./dist/.nojekyll', '');\"",
    "preview": "nuxi preview",
    "deploy": "gh-pages --dist dist --repo https://github.com/intredford/dnd"
  },
  "devDependencies": {
    "@nuxt-themes/docus": "^1.15.0",
    "@nuxt/content": "^2.10.0",
    "@nuxthq/studio": "^0.14.1",
    "gh-pages": "^5.0.0",
    "nuxt": "^3.9.1",
    "sass": "^1.59.3"
  }
}
